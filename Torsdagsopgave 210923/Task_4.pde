//task 4 a-j usikker pa opgave beskrivelsen :O
/*
class Square {
  float x, y;
  float side;

  Square(float x, float y, float side) {
    this.x = x;
    this.y = y;
    this.side = side;
  }

  void display() {
    rect(x, y, side, side);
  }
}


Square[] squares = new Square[103];

void setup() {
  size(1920, 1080);
  for (int i = 0; i < squares.length; i++) {
    float x = random(width);
    float y = random(height);
    float side = random(20, 50);
    squares[i] = new Square(x, y, side);
  }
}

void draw() {
  background(220);
  for (int i = 0; i < squares.length; i++) {
    squares[i].display();
  }
}
*/
